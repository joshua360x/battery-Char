
export function makeStatsString(headCount, middleCount, bottomCount) {

    // let headCount = 0;
    // let middleCount = 0;
    // let bottomCount = 0;


    // if (headCount === true) {
    //     headCount++
    // }
    // if (middleCount === true) {
    //     middleCount++
    // }
    // if (bottomCount === true) {
    //     bottomCount++
    // }

// console.log(headCount);
    return (`You have changed the Head State: ${headCount} times 
    Middle State: ${middleCount} times 
    Bottom State: ${bottomCount} times
    And everybody loves your character's catchphrases:`);
}